# .env (place this in your project ROOT — same folder as package.json)
# This IP is used ONLY as a bootstrap when the APK first loads.
# After that, the server returns the real IP from Windows Registry.
# Update this whenever your PC IP changes, then rebuild the APK.
VITE_SERVER_IP=192.168.25.111